// Pull in dependencies
const express = require("express");
const router = express.Router();

// Load User model
const User = require("../../models/User");

// @route GET api/profile
// @desc Register user
// @access Public
router.get("/profile", (req, res) => {
    // TODO return a res with profile info
});
